function charlotte() {

	var maVariable = 10;

alert (maVariable);
}

console.log(2+2);
//exped result: 4

document.getElementByID("lien").addventListener('contextmenu', fuction(e) ){ 
	id (prompt("ouverture du menu, confirmez : ;" ) ) 
	 });

document.getElementByID("inscription").addventListener(submit, fonction()
	{
	alert ('formualaire envoyé!');
	 
	});
